import React from "react";
 
const SignOut = () => {
    return (
        <div>
            <h1>This one probably should be "profile" and then we can have profile stuff in here and a sign out button on the page, idk tho</h1>
        <button>Sign Out (this does not do anything currently)</button>
        </div>
    );
};
 
export default SignOut;